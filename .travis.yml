matrix:
  include:
    - language: rust
      rust:
        - stable
        - nightly
    - language: node_js
      node_js:
        - "8"
  allow_failures:
    - rust: stable

sudo: false

dist: trusty

group: edge

