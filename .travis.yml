matrix:
  include:
    - language: rust
      rust:
        - stable
        - nightly
      script:
        - cargo build
        - cargo test
    - language: node_js
      node_js:
        - "8"
      script:
        - npm test
  allow_failures:
    - rust: stable

sudo: false

dist: trusty

group: edge

